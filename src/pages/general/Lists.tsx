import React from "react";
import "../general/styles.css";

interface ListProps {
  activeTab: "chars" | "planets";
}

export const List: React.FC<ListProps> = ({ activeTab }) => {
  return <></>;
};
